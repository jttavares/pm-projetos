
//pegar numeros digitados e 
let botao=document.querySelectorAll("button");

botao.addEventListener("click",function(botao)=>{
    botao.forEach(Function numeroDoBotao(botao){
       console.log(botao.innerHTML); 
    });
});






//converter em um valor decimal de duas casas automaticamente

    //* no clicar do botao converter o valor da tag para numero
        //*addEeventListener para o click
        //*capturar valor do campo
        //*compor o numero digitado
        //*já colcocar em numero decimal automaticamente
            //-descobrir qual função faz isso
                //-ou pegar o numero e dividir por 100

//requisitar valor corrente do dolar por api
    //*usar request

//função para calcular a conversão real-dolar e mostrar resultado
    //numero digitado*dolar

//formatar numeros no display
    //inicial azul
    //segundo estado=laranja
    //terceiro estado=verde

//funcao botao Clear

